#!/bin/sh

# ----------------------------------------------------
# Delete (one or multiple) selected application(s)
# ----------------------------------------------------

packages=$(brew leaves | fzf --multi --preview="")

if [ "$packages" ]; then
    for pkg in $packages
    do
        brew uninstall "$pkg"
    done
fi
