#!/bin/sh

# ----------------------------------------------------
## Install (one or multiple) selected application(s)
# ----------------------------------------------------
packages=$(brew search "$@" | fzf --multi --preview="")

if [ -n "$packages" ]; then
    for pkg in $packages
    do
        brew install "$pkg"
    done
fi
