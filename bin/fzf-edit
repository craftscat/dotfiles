#!/bin/sh

# ---------------------------------------
# fzf the file and edit it with vim
# ---------------------------------------
IFS=$'\n' files=($(fd --type file -I -H -E '.git/'| fzf-tmux --query="$1" --multi --select-1 --exit-0))
[[ -n "$files" ]] && ${EDITOR:-vim} "${files[@]}"
